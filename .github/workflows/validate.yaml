name: Validate Changes 
  on: 
    push:
      branches: 
        - main - develop jobs: validate:

  runs-on: ubuntu-latest 
      steps: 
        - name: Checkout Code 
            uses: actions/checkout@v3
        - name: Run Validation 
            run: echo "Validating code..."